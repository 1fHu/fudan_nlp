# 几何碰撞（clash）优化方案记录

> 背景：第二阶段已有数据——`rfd_c2_design/`（23 个体系的 C2 对称 de novo 设计）+ `outputs_partial/`（partial diffusion 结果）。
> 当前卡点：**物理几何失败**（链间穿插碰撞），而非置信度失败（pLDDT/PAE 都好看）。
> 本文档记录诊断口径、可落地的优化方法、调研思路，作为后续实验依据。

---

## 0. 问题定性

失败是**几何/物理可行性**问题，不是折叠置信度问题。以 `6qv9` C2 对称体为例：

| 指标 | 当前值 | 健康/天然值 |
|---|---|---|
| 链间最近骨架原子距离 | **0.79 Å** | ≥ 3.5 Å |
| 原子对距离 < 2.0 Å（真实 clash） | **34 对** | 0 |
| 两个 Mn 中心间距 | **9.35 Å** | 18–25 Å（天然 Mn-SOD dimer） |

**结论**：两条链不是"形成 dimer 界面"，而是物理穿插。任何 MD 都会爆，湿实验无法折叠。
**红线**：只看 pLDDT 必被误导，质检必须 = 结构置信度 + 物理几何，缺一不可。

---

## 1. 质检红线（先固化，自动化）

每个 design 出来先过以下硬门槛，再谈可用：

1. `链间最近骨架原子距离 ≥ 3.5 Å`
2. `clash 原子对(<2.0 Å) 数量 = 0`
3. `金属-金属间距 ∈ 天然分布区间`（Mn-SOD 约 18–25 Å）
4. （沿用）align RMSD < 1.0 Å 可用；> 2.0 Å 剔除
5. （沿用）pLDDT / PAE 高置信度

> 落地：用 PyMOL `find_pairs polymer, polymer, mode=0, cutoff=2` 数 clash；
> 或用无 PyMOL 的纯 Python（biotite/biopython + numpy）批量扫全部体系，见 `scripts/geom_qc.py`。

---

## 2. 可落地的优化方法（按性价比排序）

### 方案 A：调 RFdiffusion 引导势（最先试，仅改参数，零额外工具）
当前 `weight_inter:0.1` 太弱 → 链间约束不足 → 几何放飞穿插。调整：
- 提高 `guide_scale`（2.0 → 3~5），加大 `weight_inter`，强化链间接触势；
- `olig_contacts` 支持 attractive/repulsive → **显式加链间排斥项防穿插**，界面区保留吸引；
- 对金属中心加**自定义距离势**（metal-metal 软约束），把 Mn-Mn 直接拉向 18–25 Å —— 最对症，因为 9.35 Å 本质是两个活性中心被挤到一起；
- contigs 给两链间留更长 linker/spacer，避免初始布局就重叠。
- 参数网格：`guide_scale ∈ {2,3,4,5} × weight_inter ∈ {0.1,0.3,0.5,1.0} × guide_decay ∈ {quadratic, linear}`。

### 方案 B：后处理松弛（对已有 design 立即可用，去 clash 标配）
RFD 只产骨架，碰撞用物理松弛消除：
- **Rosetta FastRelax**（带约束）或 **OpenMM 能量最小化**（ff14SB + 对 N/CA/C/CB 加谐振子约束，保护整体折叠与 motif 几何）；
- 推荐顺序：`RFD 骨架 → LigandMPNN 填序列 → FastRelax/OpenMM 去 clash → AF3 复折叠验证 → 过几何红线`；
- 局限：松弛只救轻度 clash；0.79 Å 深度穿插救不回来，须靠方案 A 从生成阶段避免。

### 方案 C：换设计范式（对称 de novo 不稳时）
- 已验证：**天然界面够好时跳过 RFD，直接 LigandMPNN 重设计，`6D52` 可用率 >0.85** → 自组装目标的当前最稳产出路径，作为主线。
- C2 de novo 作为探索线并行跑，但必须配 方案 A 引导势 + 方案 B 松弛 + 几何红线。

---

## 3. 调研 / LLM 闭环思路

不做一次性问答，让 agent 闭环（对应 `agent+memory+env→trajectory+report`）：
- agent 读 RFdiffusion README 的 symmetry/potentials 章节 + 我们的 `run.log`，自动建议引导势参数网格并批量提交；
- 把"几何红线脚本"接进 PyMOL 沙盒闭环，每个 design 跑完自动出 clash 报告 + 截图，按 log 选下一轮参数；
- 可调研新工具：带物理项的复折叠/打分（AF3 多 seed、Boltz）、对称 motif scaffolding 最新做法。

---

## 4. 下一步

1. 本地：跑几何质检脚本，把 23 个体系现有 design 的 clash / 金属间距全量量化，建 baseline。
2. 远程 GPU：用方案 A 参数网格对 `6qv9` 跑小批量（20 个）验证碰撞能否压下去。
3. 主线：`6D52` 直接 LigandMPNN 继续推可交付结果。

---

## 5. 实验环境（已就绪）

本地为 Apple Silicon Mac，**无 GPU / 无 conda / 无 PyMOL** → RFdiffusion/AF3 等生成步骤在远程 GPU 集群（`/mnt/shared-storage-gpfs2/...`）跑；本地负责**几何质检 / clash 量化**，作用于已有的第二阶段 PDB。

- venv: `.venv/`（Python 3.14，仅依赖 numpy，无编译/PyMOL 依赖）
- 质检脚本: `scripts/geom_qc.py`（纯 numpy 实现，自带 PDB 解析）
  - 单文件: `.venv/bin/python scripts/geom_qc.py path/to/design.pdb`
  - 批量:   `.venv/bin/python scripts/geom_qc.py rfd_c2_design --glob '**/*_symm/*.pdb' --csv qc_c2_baseline.csv`
- 自检：在 `6qv9` C2 上复现了记录中的 bb_min=0.79 Å、34 对 clash → 口径一致。

### 全量 baseline（`qc_c2_baseline.csv`）

对 `rfd_c2_design/**/*_symm/*.pdb` 全量扫描（85 个 design）：

| 类别 | 数量 | 说明 |
|---|---|---|
| 单链（C2 未生成 dimer） | 30 | 对称未真正产出二聚体 |
| 双链 | 55 | **全部 55 个都有 clash** |
| 双链 bb_min 范围 | 0.23–0.90 Å | 红线 ≥3.5 Å，全部不达标 |
| 双链 clash 对数 | 32–172 | 真实穿插 |
| 有碰撞体系 | 11 个 | 1bsm/1ix9/1jr9/1uer/1xre/1xuq/2rcv/3kky/4yip/6pro/6qv9 各 5 个 |

**结论**：碰撞是**系统性**问题，遍及所有 C2 体系，不是 `6qv9` 个例 → 必须从生成阶段（方案 A 引导势）解决，后处理松弛（方案 B）只能救轻度的。partial diffusion 输出（`outputs_partial/`）的双链则普遍无 clash、活性中心间距 ~18–20 Å（His-CA 代理），几何上更接近天然，但序列只有 polyG+motif，需 MPNN 补序列。

---

## 参考来源
- RFdiffusion 官方仓库 README（symmetry / guiding potentials）— https://github.com/RosettaCommons/RFdiffusion/blob/main/README.md
- RFdiffusion, Watson et al., 2023, Nature（对称 oligomer + 接触势）— https://www.nature.com/articles/s41586-023-06415-8
- RFdiffusion Potentials 说明 — https://huggingface.co/blog/AmelieSchreiber/rfdiffusion-potentials
- Rosetta FastRelax 结构优化 — https://www.computabio.com/rosetta-fastrelax-protein-structure-optimization.html
- BindEnergyCraft（clash 是 binder 设计常见失败模式 + OpenMM 去 clash）— https://arxiv.org/pdf/2505.21241
